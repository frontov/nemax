# Local Development

1. Copy `.env.example` to `.env`.
2. Start the stack with `docker compose up --build`.
3. Access the app through Nginx on `http://localhost:8080`.
4. Hit `http://localhost:8080/api/health` to verify the backend.
5. Extend the API and frontend from the existing typed skeleton.
